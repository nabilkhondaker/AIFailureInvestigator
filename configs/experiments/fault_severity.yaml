name: fault_severity
seed: 42
description: Severity estimation across degradation levels

severity_bins: [0.1, 0.3, 0.5, 0.7, 0.9]
data:
  path: data/synthetic
  n_samples: 1000

models:
  - severity_model

metrics:
  - mae
  - rmse
  - r2

output_dir: outputs/experiments/severity
