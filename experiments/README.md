# Experiments

Configuration-driven experiment definitions live under `configs/experiments/`.

Outputs should be written to `outputs/experiments/<name>/`.

Results are produced by running the experiment scripts; they are not hard-coded in this repository.
