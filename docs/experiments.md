# Experiments

Experiment configs under `configs/experiments/` cover:

- Baseline diagnosis
- Noise robustness
- Severity estimation
- Unseen faults
- Dataset scaling
- Feature / sensor ablation

Run via scripts in `scripts/` and write outputs under `outputs/experiments/`. Metrics are measured at runtime, not fabricated in documentation.
