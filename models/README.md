# Models

Trained checkpoints (pickle / torch) are stored here after running training scripts.

Regenerate with:

```bash
neural-failure generate-data
neural-failure train --config configs/models/fault_classifier.yaml
neural-failure train --config configs/models/anomaly_detector.yaml
```
